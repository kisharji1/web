Step 1: Run these commands in the terminal

npm init -y
npm install lodash

Step 2: Create a JavaScript file (index.js or main.js)

const _ = require('lodash');

let arr = [10, 20, 30, 40];

console.log("Original Array:", arr);
console.log("Reversed Array:", _.reverse(arr));
